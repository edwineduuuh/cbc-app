{
  "build": {
    "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --no-input"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT --timeout 120 --workers 2"
  }
}
